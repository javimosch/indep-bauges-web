{
  "name": "indep-bauges",
  "version": "1.0.0",
  "description": "Website for Indépendants en Bauges",
  "main": "src/server.js",
  "scripts": {
    "build": "node src/scripts/concat.js",
    "serve": "node src/server.js",
    "start": "npm run build && npm run serve",
    "dev": "npx nodemon --exec 'node src/server.js' --watch src -e js,html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "bauges",
    "website",
    "html"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "jsdom": "^24.1.3",
    "jsonwebtoken": "^9.0.2"
  }
}
